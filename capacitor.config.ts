import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firebase__rest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
